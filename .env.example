# ============================================================
# Variables de entorno para el Backend Ventas (MySQL + Spring Boot)
# Copiar como .env y completar con valores reales
# NO subir el archivo .env al repositorio
# ============================================================

# Base de datos MySQL
# DB_ENDPOINT se resuelve automáticamente como "db" (nombre del servicio Docker)
DB_NAME=citt_ventas
DB_USERNAME=innovauser
DB_PASSWORD=<password_seguro_aqui>

# Puerto donde se expone la API (debe coincidir con server.port=8082)
BACKEND_PORT=8082

# Imagen Docker Hub (usuario/nombre-imagen)
DOCKER_IMAGE=tuusuario/innovatech-ventas-backend:latest
